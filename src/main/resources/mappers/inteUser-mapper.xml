<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="inteuserMapper">


	<select id="loginCheck" parameterType="InteUser" resultType="InteUser">
		select * from inteuser where userid = #{ userid } and userpwd = #{userpwd}
	</select>
	
	<insert id="insertUser" parameterType="InteUser">
		insert into inteuser values(#{ userid }, #{userpwd}, sysdate, #{username}, #{phone}, #{email}, #{address}, default, null, null)
	</insert>
	
</mapper>








